workflows:
  android-kotlin:
    name: Android AutoClicker
    max_build_duration: 10
    environment: {}
    scripts:
      - ./gradlew assembleRelease
    artifacts:
      - app/build/outputs/**/*.apk
